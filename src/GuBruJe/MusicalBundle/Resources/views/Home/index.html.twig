{% extends 'GuBruJeCoreBundle::layout.html.twig' %}

{% block body %}
    <div class="jumbotron">
        <div class="container">
            <h1>Hello, world!</h1>
            <p>A bootstrap template example </p>
            <p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more &raquo;</a></p>
        </div>
    </div>
    <div class="container">
         <div class="row">
       {% for article in articles %}
           <div class="col-md-4">
               <h2>{{ article.title }}</h2>
               <p>{{article.contenu }}</p>
           </div>
       {% endfor %}

        </div>
        <hr>
   </div> <!-- /container -->
{%  endblock %}
