{% extends 'GuBruJeMusicalBundle::layout.html.twig' %}

{% block titre %}
    <h1 class="page-header">Annonces</h1>
{% endblock %}
{% block musical_body -%}
    {% for annonce in annonces %}
        <div class="blog-post">
            <h2><a href="{{ path('annonce_show', { 'id': annonce.id }) }}">{{ annonce.titre }}</a> <small>{{ annonce.rubrique }}</small></h2>
            <em>
                Le
                <time datetime="{{ annonce.date|date('c') }}"
                      itemprop="datePublished">{{ annonce.date|date('d/m/Y à  H\\hi') }}</time>
                par
                <span itemprop="author">{{ annonce.auteur }}</span>
            </em>
            <div class="well">
                <p>{{ annonce.contenu|markdown }}</p>
            </div>

        </div>
    {% else %}
        <p>Aucune annonce</p>
    {% endfor %}
{% endblock %}
